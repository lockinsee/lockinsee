# Lock In — regenerated static version

## Files
- index.html — page structure
- styles.css — layout, theme, animations
- data.js — constants, storage helpers, XP + level math
- app.js — UI behavior and rendering

## Hosting on GitHub Pages
Upload all files to the root of your repo and enable GitHub Pages.

## Notes
- no backend
- local browser save only
- mobile friendly
- plain HTML/CSS/JS modules

## Suggested next upgrades
- edit habit modal
- weekly review chart
- import/export backup
- installable PWA manifest + service worker
